spring:
  application:
    name: resources
  
  profiles:
    active: ${RMS_APP_PROFILE:local}  
    
  datasource:
    url: ${RMS_RESOURCES_DB_URL}
    user: ${RMS_RESOURCES_DB_ROLE}
    password: ${RMS_RESOURCES_DB_PASSWORD}
