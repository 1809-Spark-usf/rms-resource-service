language: java

addons:
  sonarcloud:
    organization: "1809-spark-usf"
    
jobs:
  include:
    - stage: pre-build
      script: mvn clean
  
    - stage: test
      script: mvn test
    
    - stage: build
      script: mvn package
      
    - stage: quality
      script: mvn verify sonar:sonar
